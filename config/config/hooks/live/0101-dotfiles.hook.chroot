#!/bin/bash

set -eu

cd /tmp
git clone https://github.com/atkinchris/dotfiles.git --depth=1
rsync \
  --exclude ".git/" \
  --exclude ".DS_Store" \
  --exclude "*.sh" \
  --exclude "README.md" \
  --exclude "Makefile" \
  -avh \
  --no-perms \
  dotfiles /etc/skel
rm -rf dotfiles
